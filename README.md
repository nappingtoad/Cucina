
  # Cucina Recipe Organizer

  Cucina, a recipe organizer that allows users to manage their favorite recipes.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  